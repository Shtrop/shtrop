# Фотосессии Sofia — пакет промптов по референсам

21 фотосессия по 10 кадров (210 кадров). Первые три собраны по присланным референсам — те же наряды, позы, локации и свет; S04–S06 развивают ту же линию; S07–S16 — максимальный гламурный регистр (бикини, боди, мокрый шёлк, полотенце, чулки, шёлковая простыня); S17–S21 — кружевное белье. Пакет **не публикует ничего** и не запускает GPU — это только исходники промптов и рендер брифов (`local-only`, `no-publish`).

## Что внутри

```
sofia/photosessions/
├── common/persona_lock.json      # личность Sofia, negative prompt, тех-дефолты, QA-гейты
├── sessions/01_golden_gym.json       # S01 — зал на закате          (референс 1)
├── sessions/02_cozy_night.json       # S02 — уютная ночь дома        (референс 2)
├── sessions/03_neon_selfie.json      # S03 — неоновое селфи          (референс 3)
├── sessions/04_morning_kitchen.json  # S04 — утро на кухне           (развитие 2 и 3)
├── sessions/05_emerald_rooftop.json  # S05 — изумрудный закат        (развитие 1)
├── sessions/06_rainy_hoodie.json     # S06 — дождливый день в худи   (развитие 3)
├── sessions/07..16_*.json            # S07–S16 — максимальный гламур
├── sessions/17..21_*.json            # S17–S21 — кружевное белье
├── tools/build_prompts.py        # сборка финальных промптов
├── tools/run_session.py          # прогон одной сессии через ComfyUI
├── tools/run_all.ps1             # один запуск: все сессии -> Рабочий стол\Фотосессия
├── tools/inspect_workflows.py    # какой из workflow ComfyUI годится для фото
└── build/                        # результат сборки (брифы, .txt промпты, batch.json)
```

Источник истины — файлы в `sessions/` и `common/`. Каталог `build/` пересобирается:

```bash
python3 sofia/photosessions/tools/build_prompts.py
```

## Разбор референсов → что воспроизводим

| # | Референс | Наряд | Ключевая поза | Свет / локация |
|---|----------|-------|---------------|----------------|
| 1 | Зал, тяга гантели | пыльно-сиреневый спорт-топ на тонких бретелях + широкие светло-серые джоггеры + белые кроссовки + цепочка со звездой | тяга гантели в наклоне, ладонь на скамье с полотенцем, взгляд в объектив | закат в панорамные окна, контровой тёплый свет, блеск на коже |
| 2 | Спальня, пижама | кремовый топ в мелкий розовый цветочек + шорты с сердечками и бантом + вязаные гольфы с бантиками | сидя на пятках спиной к камере, прогиб, взгляд через плечо | тёплая лампа и свечи слева, холодный ночной город справа |
| 3 | Селфи, гейминг-комната | белая рубчатая майка с розовым бантом + серые джоггеры + пушистый розовый халат с плеч | селфи с вытянутой руки, голова набок, подмигивание и мягкий pout | розово-фиолетовый неон + RGB монитора + свеча |

### Сессии 4–6 — развитие той же линии

| # | Сессия | Наряд | Ключевой кадр | Свет / локация |
|---|--------|-------|---------------|----------------|
| S04 | Утро на кухне | кремовое oversize худи с плеча + белые шорты + носки | первый глоток капучино у окна | мягкое утро через тюль, белый мрамор и дерево |
| S05 | Изумрудный закат | изумрудный спорт-топ + леггинсы, босиком | наклон вперёд на коврике, взгляд в объектив | терраса на крыше, контровой закат, город внизу |
| S06 | Дождливый день | серое укороченное худи на молнии + белый топ + гольфы | на подоконнике в наушниках, дождь по стеклу | холодный дневной свет + одна тёплая лампа |

S05 намеренно берёт изумруд из канон-палитры персоны, S04 — капучино; S06 выводит наушники в главный маркер кадра.

Каждая сессия строится вокруг хиро-кадра, который повторяет референс один в один; остальные 9 держат тот же наряд, локацию и схему света, но меняют ракурс, оптику и действие — чтобы получилась связная серия, а не 10 копий одного кадра.

Внутри сессии: 2 hero-кадра (по 8 сид-вариантов на отбор), остальные core/filler по 4 варианта.

## Самый короткий путь: одна команда

На машине студии, где подняты ComfyUI и Sofia LoRA:

```powershell
powershell -ExecutionPolicy Bypass -File tools\run_all.ps1
```

Скрипт проверяет ComfyUI на `127.0.0.1:8188`, сам находит workflow в API-формате
(ищет в `D:\AI_CONTENT\Sofia\workflows`, затем в `workflows` пакета), прогоняет все шесть
сессий и складывает кадры в `%USERPROFILE%\Desktop\Фотосессия\<сессия>\`, после чего
открывает эту папку. Параметры: `-Sessions golden_gym cozy_night` (только выбранные),
`-Variants 2` (меньше вариантов на кадр), `-Workflow <путь>` (если автопоиск промахнулся),
`-OutRoot <путь>` (другая папка), `-DryRun` (показать план без генерации).

`TODO_FROM_*` в `common/persona_lock.json` на прогон не влияют: раннер подставляет в ваш
workflow только промпт, негатив, seed и размер кадра, а sampler и веса LoRA/PuLID берутся из
самого графа. Это справочные значения.

Если автопоиск подцепил не тот граф (например видео-пайплайн i2v вместо фото-FLUX), осмотрите
все workflow и возьмите нужный:

```powershell
python tools\inspect_workflows.py D:\AI_CONTENT\Sofia\workflows
```

Скрипт для каждого файла скажет, API-формат ли это, фото это или видео, и получается ли
привязать позитив, негатив, seed и размер кадра. Дальше передайте нужный граф:
`-Workflow "<путь>"`. Если привязка не определяется автоматически, посмотрите узлы

```powershell
python tools\run_session.py --batch build\golden_gym\batch.json --workflow "<путь>" --out . --list-nodes
```

и укажите их вручную: `-Positive 6:text -Negative 7:text -SeedNode 25:noise_seed`.

## Как прогонять в студии

1. Preflight по `sofia-ai-studio-controller`: `control_flags\PUBLISHING_STATE.json`, `NO_DELETE.flag`, `PUBLISHING_DISABLED.flag`, локи, `nvidia-smi`, отсутствие активного `gpu_render`.
2. Подставить реальные значения вместо `TODO_FROM_*` в `common/persona_lock.json` — sampler, вес Sofia LoRA и PuLID берутся из актуальных persona/config файлов студии, здесь они намеренно не выдуманы.
3. Пересобрать: `python3 tools/build_prompts.py`.
4. Прогнать сессию одной командой (одна сессия за раз, один GPU job, по расписанию `gpu_resource_scheduler`):

   ```powershell
   python tools\run_session.py `
       --batch build\golden_gym\batch.json `
       --workflow D:\AI_CONTENT\Sofia\workflows\<ваш_flux_workflow>_api.json `
       --out D:\AI_CONTENT\Sofia\generated\golden_gym
   ```

   Workflow нужен в API-формате (в ComfyUI: Workflow -> Export (API)) — скрипт сам находит в графе
   узлы позитива, негатива, seed и размера кадра и подставляет туда данные из `batch.json`.
   Полезные флаги: `--dry-run` (показать план), `--shots S01-01 S01-07` (только хиро-кадры),
   `--variants 2` (урезать число вариантов), `--server` (если ComfyUI не на `127.0.0.1:8188`).
   Скрипт ничего не публикует и не перезаписывает существующие файлы; итог пишется в
   `<out>\run_manifest.json`.
5. Прогнать `photo_qa` + `identity_guardian` по каждому кадру; FAIL уходит в `photo_regen`, а не заменяется соседним кадром.
6. Публикация — только через Master Publish Gate отдельным решением владельца. Этот пакет её не касается.

## Границы контента

Все 210 кадров — одетый нон-эксплисит: бельё, купальник, боди, полотенце или простыня всегда надеты
и с непрозрачной подкладкой. Ни обнажённой натуры, ни explicit-содержания: `nudity`, `explicit content`,
`see-through fabric` остаются в негативе, и позитивные промпты проверяются на отсутствие этих токенов. Персона каноническая, 26 лет, без сходства с реальными людьми. Правила и negative prompt зафиксированы в `common/persona_lock.json` (`content_policy`).
